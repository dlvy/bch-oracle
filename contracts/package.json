{
  "name": "bch-oracle-contracts",
  "version": "1.0.0",
  "description": "Bitcoin Cash oracle smart contracts",
  "scripts": {
    "compile": "cashc oracle.cash --output artifacts/",
    "deploy": "node deploy.js",
    "publish": "node publish.js",
    "read": "node read-onchain.js"
  },
  "dependencies": {
    "cashscript": "^0.10.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "cashc": "^0.10.0"
  }
}
